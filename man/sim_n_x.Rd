% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{sim_n_x}
\alias{sim_n_x}
\title{Simulate a covariate}
\usage{
sim_n_x(data, covariate)
}
\arguments{
\item{data}{A \code{data.frame} containing at least an \code{id} column.}

\item{covariate}{A list describing the covariate, with elements:
\itemize{
\item \code{level}: Either \code{"obs"} (generate a value per observation) or \code{"id"}
(generate a value per subject, recycled to observations).
\item \code{f}: A function taking an integer \code{n} and returning \code{n} simulated
values (e.g. \code{rnorm}, \code{runif}).
}}
}
\value{
A numeric (or other type) vector of length \code{nrow(data)} representing
the simulated covariate.
}
\description{
Helper function to generate covariates at either the observation level
or the subject level. Typically used inside \code{\link[=sim_design]{sim_design()}}.
}
\examples{
df <- data.frame(id = rep(1:3, each = 2))

# Observation-level covariate
sim_n_x(df, list(level = "obs", f = rnorm))

# Subject-level covariate (same value for each subject's observations)
sim_n_x(df, list(level = "id", f = function(n) rnorm(n, mean = 50, sd = 10)))

}
\seealso{
\code{\link[=sim_design]{sim_design()}} for generating full designs with covariates.
}
