% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{sdt_classify}
\alias{sdt_classify}
\title{Classify responses in a Signal Detection Theory (SDT) task}
\usage{
sdt_classify(say_signal, is_signal)
}
\arguments{
\item{say_signal}{Integer vector (0/1). Participant responses
(1 = "signal", 0 = "noise").}

\item{is_signal}{Integer vector (0/1). True trial type
(1 = signal, 0 = noise).}
}
\value{
A list with counts and proportions:
\describe{
\item{hit}{Number of hits (\eqn{is\_signal = 1}, \eqn{say\_signal = 1}).}
\item{fa}{Number of false alarms (\eqn{is\_signal = 0}, \eqn{say\_signal = 1}).}
\item{miss}{Number of misses (\eqn{is\_signal = 1}, \eqn{say\_signal = 0}).}
\item{cr}{Number of correct rejections (\eqn{is\_signal = 0}, \eqn{say\_signal = 0}).}
\item{phit}{Proportion of hits: \eqn{hit / (hit + miss)}.}
\item{pfa}{Proportion of false alarms: \eqn{fa / (fa + cr)}.}
\item{pmiss}{Proportion of misses: \eqn{miss / (hit + miss)}.}
\item{pcr}{Proportion of correct rejections: \eqn{cr / (cr + fa)}.}
}
}
\description{
Computes the number of hits, misses, false alarms, and correct rejections
given observed responses and true trial types, along with their proportions.
}
\details{
Signal Detection Theory (SDT) classifies each trial outcome into
four categories:
\itemize{
\item \strong{Hit}: correctly saying "signal" when a signal is present.
\item \strong{Miss}: saying "noise" when a signal is present.
\item \strong{False alarm}: saying "signal" when only noise is present.
\item \strong{Correct rejection}: correctly saying "noise" when no signal is present.
}

The proportions (\code{phit}, \code{pfa}, \code{pmiss}, \code{pcr}) are useful for
computing SDT measures such as \eqn{d'} and criterion.
}
\examples{
set.seed(123)
dat <- sim_sdt(100, d = 1, c = 0)
sdt_classify(dat$say_signal, dat$is_signal)
}
\keyword{internal}
