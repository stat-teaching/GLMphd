% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{bin2binary}
\alias{bin2binary}
\title{Convert binomial data to binary format}
\usage{
bin2binary(data, nc, nt)
}
\arguments{
\item{data}{A \code{data.frame} containing the binomial data.}

\item{nc}{A column in \code{data} giving the number of successes.}

\item{nt}{A column in \code{data} giving the total number of trials.}
}
\value{
A \code{data.frame} where each row corresponds to a single binary trial,
with a new column \code{y} containing 1s (successes) and 0s (failures).
}
\description{
Expands a dataset in aggregated binomial form (successes and total trials)
into a dataset in binary form (0/1 outcomes).
}
\examples{
df <- data.frame(
  x = c(1, 2)
  successes = c(2, 3),
  trials = c(5, 4)
)

bin2binary(df, successes, trials)

# Resulting dataset has 9 rows:
# - 2 successes and 3 failures from the first row
# - 3 successes and 1 failure from the second row

}
