% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-distr.R
\name{distplot}
\alias{distplot}
\title{Plot Probability Distributions for Multiple Parameter Sets}
\usage{
distplot(dist, dist.args = NULL, n = 1000, ...)
}
\arguments{
\item{dist}{Character. Distribution name. Supported: \code{"normal"}, \code{"gamma"}, \code{"poisson"}.}

\item{dist.args}{Named list or data.frame. Distribution parameters. Each row (if \code{data.frame})
corresponds to one curve. Examples:
\itemize{
\item Normal: \code{list(mean = c(0, 2), sd = c(1, 0.5))}
\item Gamma: \code{list(shape = c(2, 5), scale = 1)}
\item Poisson: \code{list(lambda = c(1, 5, 10))}
}}

\item{n}{Integer. Number of random samples drawn per parameter set (default: \code{1000}).}

\item{...}{Additional arguments passed to \code{tinyplot::tinyplot()}.}
}
\value{
A plot of the distribution curves, faceted/colored by parameter sets.
Also invisibly returns the data frame used for plotting.
}
\description{
\code{distplot()} generates and plots probability distributions (Normal, Gamma, Poisson)
for one or more sets of distribution parameters. Each parameter set is visualized
as a separate curve with an automatically generated legend.
}
\details{
For each parameter set, \code{n} random samples are generated and their density
(or probability for discrete distributions) is computed.
Curves are drawn using \code{tinyplot::tinyplot()}, with conditioning on the
parameter combination (shown in the legend).

Internally:
\itemize{
\item \code{dnorm}, \code{dgamma}, \code{dpois} are used for densities.
\item \code{rnorm}, \code{rgamma}, \code{rpois} are used for random draws.
\item Parameter values are automatically expanded across multiple sets.
}
}
\examples{
# Normal distributions
distplot("normal", dist.args = list(mean = c(0, 2), sd = c(1, 0.5)), n = 2000)

# Gamma distributions
distplot("gamma", dist.args = list(shape = c(2, 5), scale = 1))

# Poisson distributions
distplot("poisson", dist.args = list(lambda = c(2, 5, 10)), n = 500)

}
