% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{mcoefs}
\alias{mcoefs}
\title{Extract model coefficients with confidence intervals}
\usage{
mcoefs(x, level = 0.95, ci.type = "profile")
}
\arguments{
\item{x}{A fitted model object (e.g., from \code{\link[stats]{lm}} or \code{\link[stats]{glm}}).}

\item{level}{The confidence level for intervals (default \code{0.95}).}

\item{ci.type}{The type of confidence interval to compute.
One of \code{"profile"} (default, uses \code{\link[stats]{confint.default}})
or \code{"wald"} (uses \code{\link[stats]{confint}}).}
}
\value{
A \code{data.frame} with the following columns:
\itemize{
\item \code{param} - parameter name
\item \code{beta} - estimated coefficient
\item \code{se} - standard error
\item \code{stat} - test statistic
\item \code{p} - p-value
\item \code{ci.lb} - lower bound of the confidence interval
\item \code{ci.ub} - upper bound of the confidence interval
}
}
\description{
This function returns a tidy data frame of model coefficients, standard errors,
test statistics, p-values, and confidence intervals for a fitted model.
}
\details{
\itemize{
\item \code{"profile"} computes intervals based on the profile likelihood method.
\item \code{"wald"} computes Wald-type confidence intervals.
}

The returned data frame contains one row per model parameter.
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
mcoefs(fit, level = 0.95, ci.type = "wald")

}
\seealso{
\code{\link[stats]{summary.lm}}, \code{\link[stats]{confint}},
\code{\link[stats]{confint.default}}
}
