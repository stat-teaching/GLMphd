% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{binary2bin}
\alias{binary2bin}
\title{Convert binary dataset to binomial summary}
\usage{
binary2bin(data, formula)
}
\arguments{
\item{data}{A \code{data.frame} containing the binary outcome variable \code{y} and
grouping variables.}

\item{formula}{A formula specifying the outcome and grouping variable(s),
e.g., \code{y ~ x} where \code{y} is the 0/1 variable and \code{x} is the grouping variable.}
}
\value{
A \code{data.frame} containing:
\itemize{
\item \code{nc}: Number of successes per group.
\item \code{nt}: Total trials per group.
\item \code{nf}: Number of failures per group (\code{nt - nc}).
\item The grouping variables.
}
}
\description{
Aggregates a binary dataset (0/1 outcomes) into a binomial summary dataset
with counts of successes and total trials. Useful for converting individual
trial-level data into a summarized form for modeling.
}
\examples{
df <- data.frame(
  y = c(1,0,1,1,0,0,1),
  group = c("A","A","B","B","B","C","C")
)
binary2bin(df, y ~ group)

}
