% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{plot_dfbeta}
\alias{plot_dfbeta}
\title{Plot and extract DFBETAs}
\usage{
plot_dfbeta(x, b = NULL, threshold = NULL, intercept = FALSE)
}
\arguments{
\item{x}{A fitted regression model (e.g., from \code{\link[=lm]{lm()}} or \code{\link[=glm]{glm()}}).}

\item{b}{Optional. A character vector of coefficient names to include
(defaults to all).}

\item{threshold}{Numeric. Cutoff for influence, defaults to \eqn{2/\sqrt{n}},
where \eqn{n} is the number of observations.}

\item{intercept}{Logical. Whether to include the intercept term in the plot
and results (default \code{FALSE}).}
}
\value{
A \code{data.frame} in long format containing:
\itemize{
\item \code{obs}: Observation index.
\item \code{param}: Coefficient name.
\item \code{value}: DFBETA value.
\item \code{obs_n}: Original row names of the data.
\item \code{threshold}: The threshold used for outlier detection.
\item \code{is_out}: Logical, whether the DFBETA exceeds the threshold.
}
}
\description{
Computes and plots DFBETA values for a fitted regression model, with an
optional threshold to highlight influential observations. Returns the
underlying long-format data frame used for plotting.
}
\details{
The function produces a plot of DFBETA values by parameter. Observations
exceeding the threshold are flagged in the returned data frame.
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
dfbl <- plot_dfbeta(fit)

# Only plot selected coefficient
dfbl_hp <- plot_dfbeta(fit, b = "hp")

}
\seealso{
\code{\link[stats:influence.measures]{stats::dfbeta()}} for the underlying computation of DFBETAs.
}
