% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{new_cov}
\alias{new_cov}
\title{Create a covariate specification}
\usage{
new_cov(fun, level = "obs")
}
\arguments{
\item{fun}{A function that takes an integer \code{n} and returns \code{n}
simulated values (e.g., \code{rnorm}, \code{runif}).}

\item{level}{A character string, either \code{"obs"} for observation-level
covariates (different value per row), or \code{"id"} for subject-level
covariates (same value repeated for all observations of the same subject).}
}
\value{
A list with elements \code{f} (the generator function) and
\code{level} (the specified level).
}
\description{
Helper to create a covariate object that can be passed to
\code{\link[=sim_design]{sim_design()}} or \code{\link[=sim_n_x]{sim_n_x()}}. Wraps a generating function
together with the level at which the covariate varies.
}
\examples{
# Observation-level covariate
new_cov(rnorm, level = "obs")

# Subject-level covariate
new_cov(function(n) rnorm(n, mean = 50, sd = 10), level = "id")

}
\seealso{
\code{\link[=sim_design]{sim_design()}}, \code{\link[=sim_n_x]{sim_n_x()}}
}
