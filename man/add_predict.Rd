% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-glm.R
\name{add_predict}
\alias{add_predict}
\title{Add predictions to a data frame}
\usage{
add_predict(data, fit, ...)
}
\arguments{
\item{data}{A \code{data.frame} containing the variables used in the model.}

\item{fit}{A fitted model object (e.g., from \code{\link[=lm]{lm()}}, \code{\link[=glm]{glm()}}, or other
modeling functions that support \code{\link[=predict]{predict()}}).}

\item{...}{Additional arguments passed to \code{\link[=predict]{predict()}}.}
}
\value{
A \code{data.frame} containing the original variables in \code{data} and an
additional column \code{pr} with the predictions.
}
\description{
A wrapper around \code{\link[=predict]{predict()}} that adds model predictions as a new column to
a data frame. This function is designed to work smoothly within a
pipe (\verb{\%>\%} or \verb{|>}) workflow.
}
\examples{
# Fit a simple linear model
fit <- lm(mpg ~ wt + hp, data = mtcars)

# Add predictions to the original data
head(add_predict(mtcars, fit))

# Use with pipes
mtcars |>
  add_predict(fit) |>
  head()

}
